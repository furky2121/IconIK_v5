'use client';
import { Metadata } from 'next';
import BordroDashboard from '../../../src/pages/BordroDashboard';

const BordroDashboardPage = () => {
    return <BordroDashboard />;
};

export default BordroDashboardPage;
